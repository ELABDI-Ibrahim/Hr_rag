[pytest]
# Run from the project root: pytest tests/ -v
# Adds the project root to sys.path so that `from pipeline.X import ...`
# and `from utils.X import ...` resolve correctly without package installation.
testpaths = tests
pythonpath = .
